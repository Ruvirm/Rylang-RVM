привет я прошу тебя сделать  полноценную vm   и компилятор в её байт код как у jvm она должна иметь все методы защиты , gc , стек,поддержка всех команд risk v, удобную откладку кода с крутым и подробным выводом ошибке сделай если ошибка вызвана не доставленной  { } ;  : < > то вставить в нужное место также сборку в исполняемый файл  расширения rvm которое хранит байт код программы  и исполняется vm сделай лексер. парсер. ast.  lib предпроцессор и синтаксис как у java но легче более близкий к пайтон сделай ещё отдельно набор инструментов как SDK  с предустановленными библиотеками инструментами и облегчения создания библиотек   предпроцессор может иметь файл с Препроцессор:
#undef — Отмена макроса, хотя обычно это не требуется из-за динамического контроллера препроцессора.
#define — Объявление макроса или замена слова/функции.
#определить печать cout
Особенность препроцессора в RVM:
Создайте файл с расширением .gr и определите замены:

class = класс,
cout = вывести,
int = целое число,
float = число с плавающей точкой
В основном файле сборки:

preprocessor - имя_файла;  .  сделай что sdk vm компилятор  отдельными друг от друга и что машина может исполнять  java  сделай всё как профессиональная команда